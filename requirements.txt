# ==============================================================================
# Synology Space Analyzer - Runtime-Dependencies
#
# NUR was die Anwendung im Betrieb braucht. Diese Datei landet im Docker-Image
# und im Release-Paket - Test-Werkzeug gehoert deshalb nicht hierher, sondern in
# requirements-dev.txt.
#
# Exakte Pins (==) statt Ranges: Ein Build von heute und einer von naechster
# Woche installieren dieselben Versionen. Updates sind damit ein bewusster,
# reviewbarer Commit statt einer stillen Aenderung zwischen zwei CI-Laeufen.
#
# Aktualisieren:
#   pip install -U <paket> && pip freeze | grep -i <paket>
#   -> Version hier eintragen, Tests laufen lassen, committen.
# ==============================================================================

# --- HTTP / Synology-API-Client ---
requests==2.34.2
urllib3==2.7.0
aiohttp==3.14.3

# --- Web-Backend ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
jinja2==3.1.6

# --- Scheduling & Konfiguration ---
apscheduler==3.11.3
python-dotenv==1.2.2
pyyaml==6.0.3

# --- Sicherheit (Fernet-Verschluesselung der NAS-Zugangsdaten) ---
cryptography==49.0.0

# --- CLI / System ---
rich==15.0.0
inquirer==3.4.1
psutil==7.2.2
